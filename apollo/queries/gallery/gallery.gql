query Gallery($slug: String!) {
  gallery: galleryBySlug(slug: $slug) {
    id
    name
    slug
    source {
      id
      title
      public_id
    }

    images {
      id
      name
      slug
      description
      location
      price
      date_taken
    }
  }
}
